using UnityEngine;
using UnityEngine.UI;


public class slotui : MonoBehaviour
{
  
  public int inventorySize = 10;
  public itemui[] slots;
  GameObject


    void Start()
    {

        if (item != null)
        {
            Debug.Log("Slot initialized with: " + item.itemname + " x" + quantity);
        }

    }

    // Update is called once per frame
    void Update()
    {

        public slot
        
    }
}
